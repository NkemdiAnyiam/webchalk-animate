{
  "name": "markdown-code",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean-up": "rm -rf ./dist ./.parcel-cache ./.cache",
    "out": "npm run clean-up && rm -rf node_modules",
    "build": "npm run clean-up && npx parcel@2.9.0 build ./public/*.html --no-cache",
    "start": "npm run clean-up && npx parcel@2.9.0 ./public/*.html --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "webchalk": "file:../tarballs/webchalk-0.26.0.tgz"
  },
  "@parcel/resolver-default": {
    "packageExports": true
 }
}
